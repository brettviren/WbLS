[start]
groups = buildtools, dependencies, wcsimgrp
includes = defaults.cfg, packages.cfg

[group buildtools]
packages = cmake, modules

[group dependencies]
packages = python, ipython, root, xercesc, geant
environment = group:buildtools, package:cmake

[group wcsimgrp]
packages = wcsim
environment = group:buildtools, group:dependencies
